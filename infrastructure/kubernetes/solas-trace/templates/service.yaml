apiVersion: v1
kind: Service
metadata:
  name: {{ include "solas-trace.fullname" . }}
  labels:
    {{- include "solas-trace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "solas-trace.selectorLabels" . | nindent 4 }}
