# TypeScript Openrouter Example

TypeScript tracing implementation for a `openrouter` pattern.

## Running the Example
1. Install dependencies:
   ```bash
   npm install
   ```
2. Execute the script:
   ```bash
   npm start
   ```
