# Rust Microsoft Agent Framework Example

Standalone Rust implementation showcasing OTLP trace collection for a `microsoft_agent_framework` pattern.

## Running the Example
1. Run target compilation:
   ```bash
   cargo run
   ```
2. Verify trace on `http://localhost:3000`.
