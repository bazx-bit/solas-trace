# Rust Claude Agent Sdk Example

Standalone Rust implementation showcasing OTLP trace collection for a `claude_agent_sdk` pattern.

## Running the Example
1. Run target compilation:
   ```bash
   cargo run
   ```
2. Verify trace on `http://localhost:3000`.
