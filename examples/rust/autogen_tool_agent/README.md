# Rust Autogen Tool Agent Example

Standalone Rust implementation showcasing OTLP trace collection for a `autogen_tool_agent` pattern.

## Running the Example
1. Run target compilation:
   ```bash
   cargo run
   ```
2. Verify trace on `http://localhost:3000`.
