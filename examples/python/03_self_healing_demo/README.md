# Self-Healing Diagnosis Simulation Example (Python)

This example demonstrates how to trace and diagnose failed agent steps inside the Solas Trace dashboard.

## Running the Example
1. Run the script:
   ```bash
   python main.py
   ```
2. Navigate to the dashboard (`localhost:3000`) and trace the failures.
