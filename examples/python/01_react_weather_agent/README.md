# ReAct Weather Agent Example (Python)

This example demonstrates a standard Reasoning-and-Action (ReAct) agent loop configured to use the Solas Trace proxy server.

## Running the Example
1. Run the script:
   ```bash
   python main.py
   ```
2. Navigate to the Solas Trace dashboard (`localhost:3000`) and locate the corresponding `trace-react-*` run.
